(function(){'use strict';if(!matchMedia('(hover: hover) and (pointer: fine)').matches)return;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;document.documentElement.classList.add('connor-cursor-enabled');const core=document.createElement('span'),ring=document.createElement('span');core.className='connor-cursor-core';ring.className='connor-cursor-ring';document.body.append(core,ring);let tx=innerWidth/2,ty=innerHeight/2,rx=tx,ry=ty,lastX=tx,lastY=ty,lastTime=0,visible=false;function dust(x,y){if(reduced)return;const now=performance.now();if(Math.hypot(x-lastX,y-lastY)<13||now-lastTime<28)return;lastX=x;lastY=y;lastTime=now;const mote=document.createElement('i');mote.className='connor-cursor-dust';mote.style.left=x+'px';mote.style.top=y+'px';mote.style.setProperty('--size',1.5+Math.random()*3+'px');mote.style.setProperty('--alpha',.28+Math.random()*.42);mote.style.setProperty('--life',520+Math.random()*420+'ms');mote.style.setProperty('--drift-x',-14+Math.random()*28+'px');mote.style.setProperty('--drift-y',12+Math.random()*25+'px');document.body.appendChild(mote);mote.addEventListener('animationend',()=>mote.remove(),{once:true})}function animate(){const ease=reduced?1:.16;rx+=(tx-rx)*ease;ry+=(ty-ry)*ease;core.style.transform=`translate3d(${tx}px,${ty}px,0)`;ring.style.transform=`translate3d(${rx}px,${ry}px,0)${ring.classList.contains('is-view')?' rotate(45deg)':''}`;requestAnimationFrame(animate)}document.addEventListener('pointermove',event=>{tx=event.clientX;ty=event.clientY;if(!visible){visible=true;core.classList.add('is-visible');ring.classList.add('is-visible')}dust(tx,ty)},{passive:true});document.addEventListener('pointerover',event=>{const interactive=event.target.closest('a,button,input,textarea,select,[role="button"],.member-card,.memory-card,.lineage-person,.gallery-item');const visual=event.target.closest('img,.memory-card,.lineage-person,.gallery-item');core.classList.toggle('is-interactive',!!interactive);ring.classList.toggle('is-interactive',!!interactive);ring.classList.toggle('is-view',!!visual)});document.addEventListener('pointerdown',()=>ring.classList.add('is-down'));document.addEventListener('pointerup',event=>{ring.classList.remove('is-down');if(reduced)return;const impact=document.createElement('i');impact.className='connor-cursor-impact';impact.style.left=event.clientX+'px';impact.style.top=event.clientY+'px';document.body.appendChild(impact);impact.addEventListener('animationend',()=>impact.remove(),{once:true})});document.documentElement.addEventListener('mouseleave',()=>{visible=false;core.classList.remove('is-visible');ring.classList.remove('is-visible')});addEventListener('blur',()=>{core.classList.remove('is-visible');ring.classList.remove('is-visible')});animate()})();
