(function(){'use strict';document.body.classList.add('ariani-legal-mode');const root=document.querySelector('.legal-chambers');if(!root)return;const tabs=[...root.querySelectorAll('.legal-tab')],panels=[...root.querySelectorAll('.legal-panel')];function activate(name,focus=false){tabs.forEach(tab=>{const active=tab.dataset.legal===name;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',String(active));tab.tabIndex=active?0:-1;if(active&&focus)tab.focus()});panels.forEach(panel=>{const active=panel.id===`legal-${name}`;panel.hidden=!active;panel.classList.toggle('is-active',active)})}tabs.forEach((tab,index)=>{tab.addEventListener('click',()=>activate(tab.dataset.legal));tab.addEventListener('keydown',event=>{if(!['ArrowLeft','ArrowRight'].includes(event.key))return;event.preventDefault();const direction=event.key==='ArrowRight'?1:-1;const next=(index+direction+tabs.length)%tabs.length;activate(tabs[next].dataset.legal,true)})});activate('formation');const cursor=document.createElement('div');cursor.className='legal-cursor';cursor.textContent='§';cursor.setAttribute('aria-hidden','true');document.body.appendChild(cursor);document.addEventListener('pointermove',event=>{cursor.style.left=`${event.clientX}px`;cursor.style.top=`${event.clientY}px`},{passive:true});document.querySelectorAll('a,button,.profile-card,.case-document').forEach(element=>{element.addEventListener('pointerenter',()=>cursor.classList.add('is-over'));element.addEventListener('pointerleave',()=>cursor.classList.remove('is-over'))});document.addEventListener('pointerdown',event=>{if(event.pointerType==='touch')return;const stamp=document.createElement('i');stamp.className='legal-stamp';stamp.textContent='REGISTRADO';stamp.style.left=`${event.clientX}px`;stamp.style.top=`${event.clientY}px`;document.body.appendChild(stamp);setTimeout(()=>stamp.remove(),850)})})();
